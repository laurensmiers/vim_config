

" disable vi compatibility
set nocompatible

" enable auto-read -- detect when file is changed from outside
set autoread

" draw line under current line
set cursorline

" remember commands
set history=10000

" start searching while typing
set incsearch

" highlight search results
set hlsearch

